//===============================================
#include "GDelay.h"
//===============================================
#define TIME_MS 127
//===============================================
void GDelay_ms(uint ms) {
	uint i, j;
		for(i = 0; i < ms; i++) {
			for(j = 0; j < TIME_MS; j++);
	}
}
//===============================================
