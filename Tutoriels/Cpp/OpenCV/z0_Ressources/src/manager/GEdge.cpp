//================================================
#include "GEdge.h"
//================================================
GEdge* GEdge::m_instance = 0;
//================================================
GEdge::GEdge() {

}
//================================================
GEdge::~GEdge() {
	
}
//================================================
GEdge* GEdge::Instance() {
	if(m_instance == 0) {
        m_instance = new GEdge;
	}
	return m_instance;
}
//================================================
void GEdge::canny(const cv::Mat& src, cv::Mat& dst) {
    cv::Canny(src, dst, 10, 100, 3, true);
}
//================================================
