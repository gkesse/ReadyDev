//===============================================
function openSitemapTab(obj, name) {
	GSitemap.Instance().openSitemapTab(obj, name);
}
//===============================================
function enumerateUrl() {
	GSitemap.Instance().enumerateUrl();
}
//===============================================
function enumerateClear() {
	GSitemap.Instance().enumerateClear();
}
//===============================================
function listUrl() {
	GSitemap.Instance().listUrl();
}
//===============================================
function listClear() {
	GSitemap.Instance().listClear();
}
//===============================================
function generateUrl() {
	GSitemap.Instance().generateUrl();
}
//===============================================
function generateClear() {
	GSitemap.Instance().generateClear();
}
//===============================================
function visualizeUrl() {
	GSitemap.Instance().visualizeUrl();
}
//===============================================
function visualizeClear() {
	GSitemap.Instance().visualizeClear();
}
generateUrl//===============================================
