//===============================================
function loadSummary1(id) {
    setTimeout(function() {
        GLoader.Instance().loadSummary1(id);
    }, 200);
}
//===============================================
function loadSummary2(id) {
    setTimeout(function() {
        GLoader.Instance().loadSummary2(id);
    }, 200);
}
//===============================================
function loadList1(id, file, key) {
	GLoader.Instance().loadList1(id, file, key);
}
//===============================================
function loadList2(id, file, key) {
	GLoader.Instance().loadList2(id, file, key);
}
//===============================================
function loadList3(id, file, key) {
	GLoader.Instance().loadList3(id, file, key);
}
//===============================================
function loadList4(id, file, key) {
	GLoader.Instance().loadList4(id, file, key);
}
//===============================================
function loadList5(id, file, key1, key2) {
	GLoader.Instance().loadList5(id, file, key1, key2);
}
//===============================================
function loadList6(id, file, key1, key2) {
	GLoader.Instance().loadList6(id, file, key1, key2);
}
//===============================================
function loadData1(id, file, key) {
	GLoader.Instance().loadData1(id, file, key);
}
//===============================================
function loadData2(id, file, key1, key2) {
	GLoader.Instance().loadData2(id, file, key1, key2);
}
//===============================================
function loadData3(id, file, key1, key2) {
	GLoader.Instance().loadData3(id, file, key1, key2);
}
//===============================================
function loadPdf1(id, file, key) {
	GLoader.Instance().loadPdf1(id, file, key);
}
//===============================================
function loadFileLink(id, file) {
	GLoader.Instance().loadFileLink(id, file);
}
//===============================================
