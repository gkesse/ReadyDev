//================================================
function onItemClick(obj, req, params = "") {
    GManager.Instance().run(obj, req, params);
}
//================================================
